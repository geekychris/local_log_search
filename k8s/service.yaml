apiVersion: v1
kind: Service
metadata:
  labels:
    app: local-log-search
    app.kubernetes.io/managed-by: devportal
  name: local-log-search-ui
spec:
  type: NodePort
  selector:
    app: local-log-search
  ports:
  - name: http
    port: 8080
    targetPort: http
    protocol: TCP
    nodePort: 30010
